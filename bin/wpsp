#!/usr/bin/env php
<?php
require __DIR__ . '/../vendor/autoload.php';

use Symfony\Component\Console\Application;
use WPSP\Funcs;

// Define variables.
$application   = new Application();
$rootNamespace = Funcs::instance()->getRootNamespace();
$mainPath      = Funcs::instance()->getMainPath();

// Init vendor commands.
\WPSPCORE\Console\Kernel::initCommands($application, $rootNamespace, $mainPath);

/**
 * Your custom commands define below here.
 */

//$application->add(new \WPSP\app\Console\Commands\YourCustomCommand(null, $rootNamespace, $mainPath));

try {
	$application->run();
}
catch (Exception $e) {
	_debug($e, true);
}